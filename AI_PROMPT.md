# AIエージェント実行プロンプト: ApexStatsOCR パイプライン実行・検証

このファイルは、AIコーディングエージェント（Claude Code等）にApexStatsOCRのOCRパイプラインを
実行・検証させるためのプロンプトです。このファイルの内容をそのままエージェントへ指示として渡せば、
以下の手順を自律的に実行できます。

---

## あなたの役割

あなたはApexStatsOCRリポジトリのルートディレクトリで作業するAIコーディングエージェントです。
Apex Legendsの成績画面スクリーンショットをOCRしてCSV化するパイプラインを実行し、
出力結果に異常がないか検証してください。

## 事前条件の確認

1. カレントディレクトリがこのリポジトリのルート（`main.py`, `config.json` が存在する場所）であること
2. `input/` ディレクトリに `<シーズン番号>.png`（例: `15.png`）形式の画像が1枚以上存在すること
   - 存在しない場合は処理を中断し、ユーザーに画像の配置を依頼する
3. 依存パッケージがインストール済みか確認し、未インストールなら導入する

   ```bash
   pip install -r requirements.txt
   ```

4. インターネット接続があることを確認する（初回実行時、EasyOCRが認識モデルをネット経由でダウンロードするため。
   オフライン・プロキシ制限下では失敗する）

## 実行手順

1. OCRパイプラインを実行する

   ```bash
   python main.py
   ```

2. 標準出力・`ocr.log` を確認する
   - `ERROR` ログ（例: `領域エラー`, `画像読込失敗`）が出ていないか確認する
   - 出ていた場合、対象画像ファイル名と項目名を記録する

3. `ocr.log` 末尾の `異常値検出` の `WARNING` ログを確認する
   - `main.py` は出力後に自動で型チェック・累積値の単調性チェック・統計的外れ値検知（MADベースの
     修正z-score、閾値は `config.json` の `anomaly_detection.mad_z_threshold`）を行い、
     疑わしい値があれば `season=... column=... value=... rule=... detail=...` の形式で警告する
   - `異常値検出: 問題ありません` であれば4は不要

4. 異常（`rule=type` / `monotonicity` / `statistical_outlier`）が検出された場合
   - 該当列名に対応する `debug/<列名>.png`（例: `debug/season_kdr.png`）を確認し、
     OCR切り出し領域のズレ・文字潰れ・解像度不足がないか調べる
   - 原因が座標ズレと判断できる場合は `config.json` の `regions` 座標、または `ocr.resize_scale` を
     調整し、再度 `python main.py` を実行して改善したか確認する
   - 原因不明・画像側の問題（背景と数字の色被り等）の場合は、修正せずに検証結果として報告する

5. 最後に、検証結果を簡潔に報告する
   - 処理した画像枚数、異常が見つかった項目・シーズン、修正した設定があればその内容

## 出力物（このパイプラインが書き換えるファイル）

- `output/apex_stats.csv` — シーズン別スタッツCSV（実行のたびに上書き）
- `ocr.log` — 実行ログ（追記式。過去の実行履歴が残り続ける）
- `debug/*.png` — OCR時に切り出した各項目の領域画像（実行のたびに上書き）

## 注意事項

- `output/apex_stats.csv` は既存データを上書きするため、既に手動修正済みのCSVがある場合は
  実行前にバックアップを確認する
- `legacy/old_main.py` / `legacy/test2.py` は現行の `config.json` と座標定義形式が異なる旧実装であり、
  このパイプラインの対象外（実行しないこと）
- ダッシュボード表示（`apex_dashboard_v3.html`）はブラウザで開いてCSVを読み込むだけで、
  AIエージェントが自動操作する対象ではない
